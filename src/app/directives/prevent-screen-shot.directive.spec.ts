import { PreventScreenShotDirective } from './prevent-screen-shot.directive';

describe('PreventScreenShotDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventScreenShotDirective();
    expect(directive).toBeTruthy();
  });
});
